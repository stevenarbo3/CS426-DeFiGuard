import { getEnvironmentsFromArgs } from "../../../common/index.js";
import * as logger from "../../../logger/console.js";
import { getSnapshotProposalData } from "./common.js";
export async function getSnapshotProposals(client, args) {
    const { pagination, filters } = args ?? {};
    const environments = getEnvironmentsFromArgs(client, args);
    const logId = logger.start("getSnapshotProposals", "Starting to get snapshot proposals...");
    const proposals = await getSnapshotProposalData({
        environments,
        filters,
        pagination,
    });
    logger.end(logId);
    return proposals;
}
//# sourceMappingURL=getSnapshotProposals.js.map