import { getEnvironmentsFromArgs } from "../../../common/index.js";
import * as logger from "../../../logger/console.js";
import { getMorphoMarketsData } from "./common.js";
export async function getMorphoMarkets(client, args) {
    const environments = getEnvironmentsFromArgs(client, args);
    const logId = logger.start("getMorphoMarkets", "Starting to get morpho markets...");
    const result = await getMorphoMarketsData({
        environments: environments,
        includeRewards: args?.includeRewards,
    });
    logger.end(logId);
    return result;
}
//# sourceMappingURL=getMorphoMarkets.js.map